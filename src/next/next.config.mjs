/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/docs',
    // output: 'export',
    // distDir: '../../dist/next',
    distDir: './dist',
  };
  
export default nextConfig;